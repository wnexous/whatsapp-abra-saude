import DeployAdapter from "../adapter"
import DeployController from "../controllers"

export const DeployInstances = () => new Promise((resolve, reject) => {
    // const adapters = DeployAdapter()
    const controllers = DeployController()
})
