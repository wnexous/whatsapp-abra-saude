import { NexusArrowFunct } from "nexus-wa/global/interfaces/messageController";

const RedeCredenciada: NexusArrowFunct = (props) => {

    return [

    ]
}

export default RedeCredenciada

const menuRedeCredenciada = () => `
REDE CREDENCIADA

Digite o nome da cidade

`